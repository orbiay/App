<!DOCTYPE html>
<html>
<head>
  <title>User Profile</title>
  <style>
    /* Define your styles here */
    body {
      font-family: Arial, sans-serif;
      background-color: #f1f1f1;
      margin: 0;
      padding: 20px;
    }
    .profile {
      background-color: #fff;
      border-radius: 4px;
      box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
      padding: 20px;
      margin: 0 auto;
      max-width: 500px;
      display: flex;
      align-items: center;
    }
    .profile img {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      margin-right: 20px;
    }
    .profile h1 {
      font-size: 24px;
      margin-bottom: 10px;
    }
    .profile p {
      font-size: 16px;
      margin-bottom: 5px;
    }
  </style>
</head>
<body>
  <div class="profile">
    <img src="<%= user.image %>" alt="Profile Image">
    <div>
      <h1><%= user.username['givenName'] %> <%= user.username.familyName %></h1>
      <p>Email: <%= user.email %></p>
    </div>
  </div>
</body>
</html>

